你是 Λόγος Agent 项目的接任开发者。以下是当前项目状态的完整交接信息，请严格按此执行，不要自行假设或猜测。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【项目背景】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Λόγος 是 Nous Lab 的 AI 助手，采用 Dify + 外部引擎架构。
- Dify：大模型编排 + Agent 逻辑
- 外部引擎（本项目）：Query Rewriting → Query Planner → 多源混合检索 → RRF 融合
- 项目路径：D:\precision_agent

当前处于 Phase 3 开发阶段（知识库搭建），Phase 2 核心链路已验收通过。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【已完成的工作（不要重复做）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ Query Rewriting 层
   - 文件：core/rewrite.py
   - 模型：deepseek-v4-flash，temperature=0.3
   - 每个子查询改写为 1-3 个检索变体，JSON 保底，审计日志齐全

2. ✅ Query Planner API
   - 文件：api/planner.py
   - 模型：deepseek-v4-flash

3. ✅ 多源混合检索
   - Bing + 百度 + Chroma（已接入）+ Dify KB（占位）
   - Bing 和百度均使用 DrissionPage 浏览器自动化

4. ✅ RRF 融合 + 语义去重
   - 文件：core/fusion.py
   - 阈值 0.85，基于 SequenceMatcher

5. ✅ 内容质量过滤
   - 文件：api/retrieve.py
   - 硬过滤 content < 200 字的结果

6. ✅ 浏览器反爬绕过
   - Bing：DrissionPage 交互模式（首页→搜索框→输入→点击搜索）
   - 百度：DrissionPage 交互模式（同上，已从 requests 升级）
   - 单例复用 + 健康检查（连接断开自动重建）

7. ✅ 循环导入修复
   - fetch_page 在 search_bing.py / search_baidu.py 内函数级延迟导入

8. ✅ Phase 2 验收通过
   - 召回率提升 +33.3%（目标 ≥8%）
   - 多源生效：Bing + 百度双源正常返回
   - 改写生效：3 个变体正常生成

9. ✅ Chroma 向量库接入（骨架完成，有 bug 待修）
   - core/chroma_client.py：Chroma 连接 + Ollama bge-m3 Embedding 封装
   - core/document_loader.py：DOCX/PDF/TXT/Excel 解析 + 智能分段
   - core/search_chroma.py：真实检索实现（替换空列表）
   - ingest.py：一键导入脚本（增量检测、向量化入库）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【待你完成的任务（按优先级排序）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 P1 — 短期：

1. 修复 ingest.py 的 KeyError: 'hash' bug
   - 根因：meta 变量被 Chroma metadata 字典覆盖，导致后面 meta['hash'] 找不到
   - 修复：将 Chroma metadata 变量改名为 doc_meta 或其他名称
   - 当前状态：ingest.py 第 139 行报错，165 个 chunks 已生成但无法入库

2. 验证 Chroma 检索链路
   - 修复后运行 ingest.py 成功入库
   - 运行测试脚本验证 search_chroma 返回非空结果
   - 验证 search_all_sources 中 Chroma 结果能正常参与 RRF 融合

3. Dify KB 接入
   - search_dify_kb 当前返回空列表
   - 需调用 Dify 知识库检索 API
   - 注意：Deep Lane 不走 Dify KB，Fast Lane 可用

4. Chrome 进程残留自动清理
   - 用户计划：计数器累积到 5 次搜索后自动运行清理代码
   - 建议实现：在 _get_page() 中增加搜索计数器，满 5 次调用 taskkill + time.sleep(2)

🟢 P2 — 长期：

5. main.py reload=True 处理
   - 当前 main.py 第 46 行 uvicorn.run(..., reload=True)
   - 生产环境建议改为 reload=False，或增加环境变量控制

6. 浏览器池化（当前单例复用，高并发时可能瓶颈）
7. Query Rewriting 质量监控（审计日志已记录，可定期分析）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【关键设计决策（必读，防止幻觉）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 【Bing 和百度都必须用 DrissionPage，不能用 requests】
   - 百度已从 requests 升级为 DrissionPage，不要改回 requests
   - Bing 检测"访问模式"，必须模拟真实用户：首页 → 搜索框 → 输入 → 点击搜索
   - 百度对复杂查询启用安全验证页，requests 返回空
   - 不要尝试用 requests/curl_cffi 替代任何一方

2. 【Bing 和百度拆分为独立文件】
   - 两者反爬策略不同，拆分为独立文件避免误改
   - 优化 Bing 时只改 search_bing.py，不要碰 search_baidu.py
   - 优化百度时只改 search_baidu.py，不要碰 search_bing.py

3. 【内容过滤阈值 200 字是核心防线】
   - 这是防止垃圾 snippet 流入的关键
   - 不要随意降低，否则"新的意思"类垃圾会回流

4. 【模型名统一为 deepseek-v4-flash】
   - 设计文档 v0.2.1 明确要求
   - 涉及文件：planner.py、rewrite.py、llm.py

5. 【浏览器单例 + 健康检查】
   - _get_page() 内通过 page.title 检测连接状态
   - 断开则 quit() + 重建，重建前自动 taskkill 残留进程
   - 首次启动约 2-3 秒，后续复用

6. 【循环导入已修复】
   - fetch_page 在 search_bing.py / search_baidu.py 内函数级导入
   - 禁止恢复为模块顶层导入

7. 【Chroma 是 Deep Lane 私有知识库主力】
   - Dify KB 留给 Fast Lane，Deep Lane 不走 Dify KB
   - Embedding 用 Mac Studio Ollama bge-m3，通过 Tailscale 内网调用
   - Chroma 数据库存放在机械革命本地 D:\precision_agent\chroma_db