【Λόγος Agent 项目交接 — 新 Kimi 必读】

你是 Kimi，正在接手 Nous Lab · Λόγος Agent 项目的开发工作。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【必须阅读的文件】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Logos_Design_Document_v0.2.1.md — 设计文档（已锁定，不可擅自修改架构决策）
2. Handover_Document_v0.2.1.md — 上一位 Kimi 的交接文档
3. Logos_Agent_System_Prompt_v0.2.1_Phase1.md — Dify Agent 系统提示词模板（已过时，见下方说明）
4. logos_architecture_v2.png — 架构图
5. 【本交接文档】— 记录自 Handover v0.2.1 以来的所有变更与发现

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【已确认的事实（不可质疑/不可修改）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 工程文档 v0.2.1 已锁定，任何架构变更必须经用户确认
2. SearXNG 已永久放弃，搜索层当前使用 Bing 网页爬虫 + 百度搜索爬虫
3. 外部引擎代码在机械革命 D:\precision_agent，FastAPI 端口 8000
4. Dify 自定义 Tool 已配置：deep_query_planner + deep_hybrid_retrieve，URL 为 http://100.97.236.112:8000
5. TavilySearch 已从 Dify Agent 工具列表中物理移除（用户操作）
6. 用户设备：机械革命极光Pro（开发机）、Mac Studio M4 Max（算力中心，Tailscale IP 100.120.218.98）
7. 腾讯云服务器：轻量应用服务器，公网 IP 101.42.184.53，用于 Nginx 反向代理

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【当前开发进度（严格对照工程文档）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Phase 1: Fast Lane 先行 — ✅ 已完成并验收
Phase 2: 外部引擎骨架 — 🔄 约 85%
  - ✅ /api/v1/health 正常
  - ✅ /api/v1/planner 正常（DeepSeek-V4 Flash，JSON 输出正确）
  - ⚠️ /api/v1/retrieve 功能通，但返回内容质量不达标（见 P0）
  - ✅ Query Rewriting 已集成（core/rewrite.py）
  - ✅ Bing 爬虫已修复（Session 预热 + brotli + 二次爬虫 + 正文提取 + 清洗）
  - ✅ 百度爬虫已新增（core/search.py 中 search_baidu）
  - ✅ RRF 融合 + 去重逻辑已编写（core/fusion.py）
  - ⚠️ Chroma 向量库未初始化（chroma_db 目录为空）
  - ⏳ Dify KB 搜索未实现
Phase 3-5: 未开始

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【当前待解决问题（按优先级排序）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

P0: Deep Lane 中 Agent 未正确使用外部引擎结果
  - 现象链：
    a) retrieve API 返回 retrieved_chunks，但内容含大量垃圾（如"新的意思"、"新闻首页"）
    b) Agent 拿到后判断"资料不足"
    c) 用户已移除 TavilySearch，Agent 直接宕机/输出乱码
  - 已定位根因：
    - 直接调用 search_all_sources 结果正确，但 retrieve API 返回垃圾
    - 怀疑 Query Rewriting（core/rewrite.py）把子查询改歪，导致 Bing 返回不相关内容
    - api/retrieve.py 是否已接入百度源需确认
  - 待验证：
    - 运行 test_rewrite.py 查看改写输出
    - 确认 api/retrieve.py 是否调用 search_baidu
  - 用户约束：新输出的解决方案暂时不采用，留给新 Kimi 分析处理

P0-2: Agent 系统提示词过时
  - 当前 Dify 系统提示词仍是 Phase 1 临时版本，训练 Agent "Deep Lane = 调 TavilySearch"
  - 即使 retrieve 返回正确内容，Agent 仍可能误判为"资料不足"
  - 用户已物理移除 TavilySearch，但提示词未更新

P1: Chroma 向量库未初始化
  - chroma_db 目录为空，未导入任何文档
  - 当前由 Bing+百度 兜底，暂未阻塞

P2: Token 消耗异常
  - 用户明确表示无需担心，DeepSeek 便宜，已关闭长思考模式
  - 搁置

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【硬性约束（违反即错误）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 所有代码适配 Windows（路径用 \\ 或 os.path.join）
2. Tavily 额度宝贵（50次/月），不能用于外部引擎调试（用户已移除，但约束仍有效）
3. 任何架构变更必须经用户确认
4. 不要建议购买任何付费服务
5. API 调用必须验证后再报告"通了"
6. 严格遵循 v0.2.1 设计文档，不要修改已锁定的设计决策
7. 不要修改已锁定的设计决策（Clarify 前置、Reflection 方案 B、Ollama 备用等）
8. 用户说"暂时不采用"的方案，只记录分析，不执行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【当前代码文件位置】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

D:\precision_agent\
├── main.py              # FastAPI 入口
├── .env                 # 环境变量（含 DeepSeek Key）
├── requirements.txt     # 依赖
├── api\
│   ├── health.py        # /api/v1/health
│   ├── planner.py       # /api/v1/planner
│   └── retrieve.py      # /api/v1/retrieve（⚠️ 质量不达标，需排查）
├── core\
│   ├── llm.py           # DeepSeek API 封装
│   ├── rewrite.py       # Query Rewriting 模块（⚠️ 疑似改歪查询）
│   ├── search.py        # 搜索层（Bing + 百度 + Chroma + Dify KB）
│   ├── fusion.py        # RRF + 去重
│   └── config.py        # 配置管理
└── skills\              # Skill Menu 模板（Markdown）

测试脚本（用户本地已有）：
- test_search_quality.py
- test_retrieve_api.py
- test_rewrite.py
- test_compare.py
- test_baidu.py
- test_all_sources.py
- diagnose_bing_raw.py

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【防幻觉清单】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 不要假设 SearXNG 还能用，它已废弃
- 不要假设 Chroma 已有数据，它是空的
- 不要假设 Dify 和外部引擎在同一网络命名空间，它们通过 Tailscale IP 通信
- 不要假设 Tavily 额度充足，它只有 50次/月（且已从 Agent 移除）
- 不要编造任何 API 响应格式，必须实际调用验证
- 如果用户说"暂时不采用"，不要执行，只记录和分析
- Bing 爬虫需要 Session 预热（先访问首页再搜索），否则返回反爬垃圾
- 直接调用 search_all_sources 结果正确 ≠ retrieve API 结果正确，两者路径不同

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【下一步建议方向（供参考，需用户确认后执行）】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 解决 P0: 验证 Query Rewriting 输出，确认 api/retrieve.py 是否已接入百度源
2. 解决 P0-2: 更新 Dify Agent 系统提示词，强制 Agent 必须基于 retrieved_chunks 生成报告
3. 初始化 Chroma 向量库，导入基础文档
4. Phase 2 验收：验证召回率提升 ≥8%，Agent 不再违规调用/宕机