# Λόγος Universal v5.0 — Python 依赖清单
# 安装: pip install -r requirements.txt

# ── Web 框架 ──
fastapi==0.111.0
uvicorn[standard]==0.30.0
flask==3.0.3                    # 企业微信桥接服务

# ── HTTP 客户端 ──
httpx==0.27.0
requests==2.32.3                # Dify KB 检索、桥接层

# ── 配置与数据 ──
python-dotenv==1.0.1
pydantic==2.7.0

# ── 向量数据库 ──
chromadb==0.5.0

# ── 知识图谱 ──
networkx==3.3                   # GraphRAG 核心（实体关系网络、Louvain 社区发现）

# ── 网页解析 ──
beautifulsoup4==4.12.3          # HTML 正文提取（search.py）

# ── 浏览器自动化 ──
drissionpage==4.0.0           # Bing/Baidu 搜索（端口 9223/9226）

# ── 加密 ──
pycryptodome==3.20.0            # 企业微信消息加解密（AES）
